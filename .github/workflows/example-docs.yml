# Copy to your repo's .github/workflows/docs.yml

name: Docs

env:
  USE_LOCKFILE: ${{ vars.USE_LOCKFILE }}
  ENABLE_DOCS: ${{ vars.ENABLE_DOCS }}

on:
  push:
    branches: [ master ]
  repository_dispatch:
    types: [ docs ]

jobs:
  docs:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["lts/*"]
    steps:
      - uses: actions/checkout@v6
        if: "env.ENABLE_DOCS == 'true'"

      - name: Setup
        uses: alanscodelog/actions/.github/actions/setup@main
        if: "env.ENABLE_DOCS == 'true'"
        with:
          USE_LOCKFILE: ${{ env.USE_LOCKFILE }}
          # Uncomment if your package uses Playwright:
          # INSTALL_PLAYWRIGHT: true

      - name: Build
        uses: alanscodelog/actions/.github/actions/build@main
        if: "env.ENABLE_DOCS == 'true'"

      - name: Deploy Docs
        uses: alanscodelog/actions/.github/actions/docs@main
        if: "env.ENABLE_DOCS == 'true'"
        with:
          # Set to "true" if your package has a Nuxt playground to copy as demo
          build_playground: "false"
          # Set to "true" if your package has a demo/ directory to build
          build_demo: "false"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
